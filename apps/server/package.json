{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/beatsync",
    "build": "go build -trimpath -o dist/beatsync-server ./cmd/beatsync",
    "start": "./dist/beatsync-server",
    "test": "go test ./...",
    "format": "gofmt -w cmd internal"
  }
}
